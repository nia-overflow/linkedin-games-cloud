{
  "name": "linkedin-games",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "pnpm --filter server dev",
    "scrape": "tsx scraper/src/index.ts",
    "build": "pnpm --filter dashboard build",
    "setup:profile": "tsx scripts/setup-profile.ts",
    "discover": "tsx scripts/discover-games.ts",
    "backfill": "tsx scripts/backfill-cloud.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild"]
  }
}
